# Agent logs (operational, not part of the framework)
.agents/logs/

# Stop signal file
.agents/.stop

# Local Claude Code settings (machine-specific, not for sharing)
.claude/settings.json
.claude/settings.local.json
